# 検証記録

2026-09-12時点。設計と批判的レビューを先に作成し、条件付きで最小実装へ進んだ。人による試遊と学習効果の評価はまだ行っていない。

## 動作確認

Node.js v25.9.0で `npm test` の15件が成功した。初期値、複数の復旧経路、偶然成功、過剰調査、scale-outの一時改善と遅延悪化、DB増強/pool制限、費用、証拠スナップショット、古い証拠、30分での未完了行動の打ち切り、終了後の操作禁止、JSON出力を対象とした。

確認中に、回復直後から安定時間を数えてしまう問題を修正した。健全化してから丸3分が必要になる。また、複数介入を挟んだ後の観測が、すべての介入を確認済みにしてしまう問題も修正した。

ブラウザで以下を操作した。

- DB観測→開発者への確認→証拠・仮説・予測・確認条件を記録→feature停止→再観測→復旧宣言→AAR。
- ECS増設完了時にP99 1.8秒、3分後にP99 4.5秒・DB接続97%・追加費用12単位となること。
- 悪化後のfeature停止→再観測→AAR→自由記述→JSON表示。表示したJSONを読み、終了理由recovered、5軸評価、7判断、入力した振り返りが含まれること。
- 導入とAARのデスクトップ表示、390px幅でのAARの折り返しと横方向のはみ出しがないこと。

ブラウザのエラーログは確認した時点で0件。アプリ内ブラウザではダウンロードイベントの完了を確認できなかったため、「保存しました」という表示を「保存を開始しました」へ修正し、JSONを表示してコピーする代替を追加・検証した。通常ブラウザでのダウンロード先への保存は未検証。

## 設計上の限界

このモデルは離散的な教材用状態遷移で、実環境の性能を予測しない。費用は相対単位、顧客影響は失敗試行数の概算。成功requestの遅延による離脱や売上は推定していない。通信相手の返答も固定文である。

自動スコアは構造化された記録の有無と証拠タグの関連性を扱う。仮説が誤っていても欄を埋めれば加点されうるため、文章の論理や反証後の更新の妥当性には人の評価が必要となる。選択肢と尺度の校正、参加者の20〜30分の体験、熟練度による差、未知障害への転移は未検証。

## YAML対応後の確認

初版の15件を維持し、YAML定義・自動検出・共通エンジン・APIの確認を加えた31件が成功した。#001と#002の同じscale-outで異なる結果になること、rollbackが万能な対処にならないこと、別シナリオへ状態が混ざらないこと、第三のYAMLだけで追加できることを検証した。入力不備の検査には、重複キー、未定義の参照、効果の型、行動ID、無効な所要時間を含む。

ブラウザでは一覧から#002へ切り替え、観測→判断記録→scale-out→継続観測→復旧宣言→AARを操作した。JSON内のID、改訂番号、定義のハッシュ、#002の原因、振り返りを確認した。#001でも増設直後1.8秒から、3分経過後4.5秒・接続97%へ悪化する挙動を再確認した。

#002は共通化を検証する比較用デモである。教材としての難度、所要時間、学習効果は未検証。READMEは英語にし、画面とシナリオのen/ja対応は今後の対応として残した。

## 2026-09-13：3シナリオの追加

#003（transaction競合）、#004（schema互換性）、#005（retry増幅）を追加した。既存31件を含む63件のテストが成功。各シナリオで根拠を伴う復旧、別の復旧経路、偶然成功、過剰調査、一時改善後の悪化、悪化後に方針を変えて復旧する経路を確認した。

追加の確認には、非互換を観測した後のrollbackによる全件失敗、互換回復後の安全なrollback、drain後の互換対応とrouting復元、受付遮断中の高速な全件失敗、外部復旧後も残るretry増幅、予算制限後の排出時間、未完了行動の一回分の費用、後の証拠が過去の採点に混ざらないことを含む。互換対応後に別の容量問題を起こした際、観測文が「両版とも正常」と誤表示する問題も修正した。

#003〜#005では仮説・目的・引用証拠・実行前の状態を照合する規則を追加した。関連証拠があっても、対応しない仮説や目的で欄を埋めただけならReasoningは4点以下となる。既に観測した危険は引用の有無にかかわらずSafetyを制限する。自由記述の意味や、規則が網羅していない合理的判断の評価は依然として人に残る。

ブラウザで全5件の一覧と新シナリオの読み込みを確認した。#003ではretry直後のP99 1.5秒・失敗率0.7%から、3分後の6.8秒・8%への悪化を確認した。#004ではversionとschemaの調査、引用付き判断、互換view適用、丸3分以上の再観測、AARを操作し、03:00の判断と09:00/13:00の結果がコメントに反映された。#005では増設完了時2.2秒、その後8秒・失敗率45%、予算制限後の回復を操作した。JSON表示にシナリオID、実行前状態、設計情報、7判断、終了状態、入力した振り返りが入ることも確認した。

AARの判断timelineと振り返りを補助スコアより先に配置した。390px幅のAARを目視し、ページ幅と内容幅がともに390pxで横にはみ出さないことを確認した。#005の確認終了時のブラウザエラーログは0件。ダウンロード保存の確認範囲は前節から変わらない。

設計資料と定義ガイドの日本語lintは指摘なし。候補一覧と検証記録には文長リズムの指摘が各1件あった。比較表と検証項目を同じ粒度で記述するため維持した。10項目の批判的レビューは各YAMLの `design.critique` に保存した。因果レビューと自動テストは完了したが、参加者による試遊、熟練度の差、学習効果は未検証である。
